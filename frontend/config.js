// Paste the "ApiUrl" value from your `sam deploy` output here.
// This is the one file you edit before your app will work.
window.API_BASE = "https://3x06aqmfi5.execute-api.ap-south-1.amazonaws.com/Prod";

// Customize your project here too — this drives the title shown
// on the page (see index.html / app.js).
window.APP_NAME = "CloudVidya Starter by Ahmed Sallu";
window.APP_THEME_COLOR = "#2f1eea"; // change this to re-theme the whole app
